services:
  bes-app:
    build: .
    command: tail -f /dev/null
    volumes:
      - .:/app
      - /app/.venv
    ports:
      - 8000:8000
    depends_on:
      - bes-rds
  bes-rds:
    image: postgres:16
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"

volumes:
  postgres_data:
